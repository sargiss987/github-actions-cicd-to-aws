
name: Java CI with Gradle
env: 
  EB_PACKAGE_S3_BUCKET_NAME : "spring-boot-application-packages"
  EB_APLICATION_NAME        : "MyApp"
  EB_ENVIORNMENT_NAME       : "Myapp-env"
  AWS_REGION_NAME           : "eu-north-1"
  DEPLOY_PACKAGE_NAME       : "github-actions-cicd-to-aws-0.0.1-SNAPSHOT.jar"
  

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 11
      uses: actions/setup-java@v2
      with:
        java-version: '11'
        distribution: 'temurin'
    - name: make gradlew exe
      run: chmod +x gradlew
    - name: test and packege with gradle
      run: ./gradlew bootJar
    - name: our jar
      run: |
       cd build/libs
       ls
       readlink -f github-actions-cicd-to-aws-0.0.1-SNAPSHOT.jar
    - name: Config AWS credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id     : ${{ secrets.MY_AWS_ACCESS_KEY }}
        aws-secret-access-key : ${{ secrets.MY_AWS_SECRET_KEY }}
        aws-region            : ${{ env.AWS_REGION_NAME }}
#     - name: Copy deplotment package into S3 bucket
#       run : aws s3 cp ${{ env.DEPLOY_PACKAGE_NAME }} s3://${{ env.EB_PACKAGE_S3_BUCKET_NAME}}/
#     - name: print massage about CI/CD happy end
#       run: echo "CI/CD pipline finished successfuly

   
    
#   test:
#     runs-on: ubuntu-latest
#     needs: [build]
    
    
      
